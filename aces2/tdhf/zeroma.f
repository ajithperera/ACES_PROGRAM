      SUBROUTINE ZEROMA(WORK,NF,NL)
      IMPLICIT DOUBLE PRECISION (A-H,O-Z)
      COMMON/IOIND/IOPT(48)
      DIMENSION WORK(NL)
      IF(IOPT(30).NE.0) WRITE(6,9765)NF,NL
9765  FORMAT(1X,'ZEROMA REACHED WITH PARMS',2I6)
      DO 1 I=NF,NL
1     WORK(I)=0.0D0
      RETURN
      END
