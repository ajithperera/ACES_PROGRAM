
      SUBROUTINE ZERSYM(Z,NLIST)
      IMPLICIT INTEGER (A-Z)
      DOUBLE PRECISION Z(1)
      COMMON /SYMINF/ NSTART,NIRREP,IRREPA(255),IRREB(255),
     &                DIRPRD(8,8)
      COMMON /SYMPOP/ IRPDPD(8,22),ISYTYP(2,500),ID(18)
cjp debug
#include "bwcc.com"
      DO 10 IRREP=1,NIRREP
C       NSYDIS=IRPDPD(IRREP,ISYTYP(2,NLIST))
C       NSYDSZ=IRPDPD(IRREP,ISYTYP(1,NLIST))
C       CALL ZERO(Z,NSYDIS*NSYDSZ)
C       CALL PUTLST(Z,1,NSYDIS,1,IRREP,NLIST)
        CALL ACES_LIST_MEMSET(IRREP,NLIST,0)
10    CONTINUE
      RETURN
      END
