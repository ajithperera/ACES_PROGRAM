This library is meant to wrap MPI subroutines and functions such that
data type recasting happens in one place versus every place. In
principle, it is only needed by FORTRAN routines since C routines have
prototypes in mpi.h that define the argument types.
