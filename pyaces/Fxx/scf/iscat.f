


      SUBROUTINE ISCAT(LENGTH,A,INDX,B)
C
C INTEGER VERSION OF CRAY SCILIB ROUTINE.
C
CEND
      IMPLICIT INTEGER (A-Z)
      DIMENSION A(1),B(LENGTH),INDX(LENGTH)
      DO 10 I=1,LENGTH
       A(INDX(I))=B(I)
10    CONTINUE
      RETURN
      END
