      SUBROUTINE FMOPOP(POP,VRT,MOPOP,NIRREP)
      IMPLICIT INTEGER (A-Z)
      DIMENSION POP(8,2),VRT(8,2),MOPOP(8)
      DO 20 IRREP=1,NIRREP
       MOPOP(IRREP)=POP(IRREP,1)+VRT(IRREP,1)
20    CONTINUE
      RETURN
      END
